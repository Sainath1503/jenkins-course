node {

  // job
  try {
    stage('build') {
      println('so far so good...')
    }
    stage('test') {
      println('A test has failed!')
      sh 'exit 1'
    }
  } catch(e) {
    // mark build as failed
    currentBuild.result = "FAILURE";

    // send slack notification
    POST https://hooks.slack.com/services/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
	Content-type: application/json
	{
	"text":"Hello, world."
	}

    // throw the error
    throw e;
  }
}
